# Configuración de automatización para Netlify
# Indica que debe compilar solo la carpeta 'frontend' y desplegar su carpeta 'dist'

[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"
