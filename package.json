{
  "name": "youtube-niche-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Estudio todo-en-uno de nichos virales de YouTube (en español).",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "vercel-build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
